services:
  api:
    build: ./api
    container_name: spc-api
    ports:
      - "8000:8000"
    restart: unless-stopped

  web:
    build: ./web
    container_name: spc-web
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped
